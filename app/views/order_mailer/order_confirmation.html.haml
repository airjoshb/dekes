!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body
    %h3 Order # #{@sale.guid}
    %h2
      You've got an order from #{@sale.email} 
    %h4 Items
    - for item in @sale.cart_ids.split(',')
      - for item in Product.find(item)
        %p= item.name
    %h3 Total: #{number_to_currency(@sale.total)}